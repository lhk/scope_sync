from setuptools import setup

setup(name='scope_sync',
      version='0.1',
      description="a tool to (de)serialize python's globals()",
      url='',
      author='Lars Klein',
      author_email='lars.klein@epfl.ch',
      license='MIT',
      packages=['scope_sync'],
      zip_safe=False)
